<div class="jumbotron">
  <%= form_for @loan do |f| %>
    <%= render "layouts/errors_messages", object: f.object %>
    <h2>Empréstimo</h2>
    <%= render "form", f: f %>
    <%= f.submit "Salvar", class: "btn btn-primary" %>
  <% end %>
</div>
